---
import { siteConfig } from "../config";

const hasExperience = siteConfig.experience && siteConfig.experience.length > 0;

const toRoute = (slug) => {
  if (slug === "triumf-experience") return "triumf";
  if (slug === "opbots-experience") return "opbots";
  return slug; // fallback
};
---

{hasExperience && (
  <section id="experience" class="p-8 sm:p-12 md:p-16 lg:p-24">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-start">
      <div class="lg:col-span-4">
        <h2 class="text-3xl sm:text-4xl md:text-5xl xl:text-7xl font-bold text-gray-900">
          Experience
        </h2>
        <div
          class="w-[75px] h-[5px] mt-2 rounded-full"
          style={`background-color: ${siteConfig.accentColor}`}
        />
      </div>

      <div class="lg:col-span-8">
        <div class="space-y-8">
          {siteConfig.experience.map((exp) => (
            <a
              href={`/experiences/${toRoute(exp.slug)}`}
              class="group relative block"
            >
              <div class="block relative p-4 sm:p-6 md:p-8 bg-gray-50 rounded-xl sm:rounded-2xl border border-gray-200 transition-all duration-300 hover:bg-white hover:shadow-xl hover:border-gray-300 hover:-translate-y-1">
                <div class="space-y-4">
                  <div>
                    <h3 class="text-xl sm:text-xl font-bold text-black mt-1">
                      {exp.company}
                    </h3>
                    <p class="text-base sm:text-xl mt-1">
                      {exp.position}
                    </p>
                  </div>

                  {exp.description && (
                    <div class="space-y-2">
                      <p class="text-base sm:text-lg text-gray-600 leading-relaxed">
                        {exp.description}
                      </p>
                      <span class="inline-block bg-black text-white text-xs sm:text-sm font-semibold px-3 py-1 rounded-full">
                        Click for more
                      </span>
                    </div>
                  )}

                  {/* show up to 3 images ONLY if they exist */}
                  {(exp.img1_1 || exp.img1_2 || exp.img1_3) && (
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                      {exp.img1_1 && (
                        <img
                          src={exp.img1_1}
                          alt={`${exp.company} photo 1`}
                          class="w-full aspect-square object-cover rounded-md shadow-lg"
                        />
                      )}
                      {exp.img1_2 && (
                        <img
                          src={exp.img1_2}
                          alt={`${exp.company} photo 2`}
                          class="w-full aspect-square object-cover rounded-md shadow-lg"
                        />
                      )}
                      {exp.img1_3 && (
                        <img
                          src={exp.img1_3}
                          alt={`${exp.company} photo 3`}
                          class="w-full aspect-square object-cover rounded-md shadow-lg"
                        />
                      )}
                    </div>
                  )}
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </div>
  </section>
)}
